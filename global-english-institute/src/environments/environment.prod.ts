export const environment = {
  production: true,
  // These are replaced at build time via Vercel environment variables
  supabaseUrl: 'YOUR_SUPABASE_URL',
  supabaseAnonKey: 'YOUR_SUPABASE_ANON_KEY',
  resendApiKey: 'YOUR_RESEND_API_KEY',
};
